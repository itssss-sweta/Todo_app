import 'package:flutter/material.dart';

class AppColors {
  // Modern vibrant primary color (Purple)
  static const Color primary = Color(0xFF6366F1);

  // Secondary color for success/completed tasks (Green)
  static const Color secondary = Color(0xFF10B981);

  // Background for light theme (Pure White)
  static const Color backgroundLight = Color(0xFFFFFFFF);

  // Background for dark theme (Rich Dark)
  static const Color backgroundDark = Color(0xFF0F172A);

  // Surface colors for cards and containers
  static const Color surfaceLight = Color(0xFFF8FAFC);
  static const Color surfaceDark = Color(0xFF1E293B);

  // Primary text color for light theme
  static const Color textPrimaryLight = Color(0xFF1E293B);

  // Primary text color for dark theme
  static const Color textPrimaryDark = Color(0xFFF1F5F9);

  // Secondary text colors
  static const Color textSecondaryLight = Color(0xFF64748B);
  static const Color textSecondaryDark = Color(0xFF94A3B8);

  // Error/Alert color (Red)
  static const Color error = Color(0xFFEF4444);

  // Warning color (Orange)
  static const Color warning = Color(0xFFF59E0B);

  // Neutral colors for borders and dividers
  static const Color borderLight = Color(0xFFE2E8F0);
  static const Color borderDark = Color(0xFF334155);

  // Accent colors
  static const Color accent1 = Color(0xFF8B5CF6); // Purple variant
  static const Color accent2 = Color(0xFF06B6D4); // Cyan
  static const Color accent3 = Color(0xFFEC4899); // Pink
}
